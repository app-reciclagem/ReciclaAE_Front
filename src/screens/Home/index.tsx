import { SafeAreaView } from 'react-native-safe-area-context';
import { Container, Text } from './styles';

export const Home = () => {
  return (
    <SafeAreaView>
      <Container>
        <Text>Home</Text>
      </Container>
    </SafeAreaView>
  );
};
